2. mkdir lab1
   cd lab1 - se kreira folder vo kojshto ke go inicijalizirame git repozitoriumot 
3. notepad l1.txt
4. git init
5. git add l1.txt
   git commit -m "Initial commit"
   
6. git add l1.txt
   git commit -m "modify l1.txt"
9. git remote add origin https://github.com/AndreaKacarska/NovRepozitorium.git
10. git push -u origin master
11. git reset --soft HEAD^
12. git checkout -b logging
13. notepad log.txt
14. git push -u origin logging 
15. git checkout master
    git merge logging 
16. git checkout -b allcommands